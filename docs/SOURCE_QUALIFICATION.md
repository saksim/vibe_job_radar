# 当前主干的源码绑定验收与候选打包

关联 #28/Q2、原 PR #29、总控 #3。基线为 PR33 已合并的 main@635755472936ec4081e3b648a836b90d7a1c5cb9。本批是独立工程交付，不修改采集、取消接口、代理、解析、登录或数据来源；不增加运行依赖，不要求部署服务器，不将版本号提升冒充产品能力提升。

## 开发者使用

在源码根目录运行 `python scripts/verify_candidate.py`。它使用当前 Python 解释器，依次实际运行全量单元测试、HTML 使用指南同步检查、离线合成演示和临时工作区的 source doctor。不会安装软件或发起真实招聘站访问。结果保存在 release-verification/result.json。

Issue #179 补充超时诊断：单测仍限600秒，其余每步仍限180秒。任一步超时后停止后续步骤，汇总和命令行保留 `TimeoutExpired`、`local_check_timeout`、`failed_check` 及 `timeout_seconds`，不再被缺少最终单测报告的次生错误覆盖。已有逐测试进度、源码是否改变和可能已经写出的测试结果仍分别保留；超时结果始终不具备打包资格。原600秒实站外CI失败及其复验单独记录，诊断改善不代表耗时根因已修复。

然后运行 `python scripts/build_candidate.py`。没有通过且精确匹配当前源码的结果时拒绝构建，不能只用浏览器截图、旧日志或另一个分支的测试数字替代。ZIP 名包含实际 ZIP 内容摘要前缀；版本仍从 src/vibe_job_radar/_version.py 读取，当前不修改已合并版本。

这两个命令是维护者的验收工具，不是普通用户获取岗位的新增操作。原网页流程不变。CI 的浏览器验收后也自动执行同一个源码验证器再打包，不要求用户手动准备云服务。

## 拒绝条件和一致性

- 对 src/scripts/tests/docs/examples/demo_output/.github 及已列明根文件建立逐文件字节 SHA-256 清单；新增、删除或改动文档同样使旧证据失效。运行目录、根 .env、缓存字节码和日志不作为源码。
- 拒绝源码符号链接及源目录中的明显私密数据库、.env、密钥文件。仅既有两个明确 test-only PEM 夹具可入包；这不是通用秘密扫描器，维护者仍须审查源码。
- 解析所有 Python 源文件并检查单一版本源。版本不能含路径分隔符；不会通过字符串拼接把文件写出候选目录。
- 测试数必须为真正的正整数，失败/错误/跳过必须全部为整数零；布尔值不能冒充计数，顶层 success=true 不能盖过失败。四步顺序、退出码和源码不变条件同时验证。
- 每次验证使用独立临时单测结果路径，旧报告不能被误用；超时/失败覆盖本次结果为失败。生产者与打包器共用严格验证函数。
- 构建时再次逐文件验证，并在结束前核对完整清单，拒绝读取期间改动。独立临时文件、原子替换及按 ZIP 摘要命名避免混用半成品；失败不删除先前的有效候选。
- CANDIDATE.json 带源码清单及本地证据；manifest.json 带 ZIP 摘要。每个候选可追溯，但它不是签名证明，不能防止有权改源码/证据的攻击者伪造。
- 明确 browser_bundled=false、live_sites_certified=false、远端 CI 需另外验证。不是免安装包、正式 Release 或实际 VPN/三站认证。

## 与原 PR29 的关系

移植 qualification/verify/build 三项实现及原20项测试目的，并对当前 Python/Windows 的显式 UTF-8、测试夹具隔离、跳过计数、严格 schema、私密文件拒绝和打包中改动新增回归。保留原单测7组合、浏览器8脚本和健康3组合；功能分支不重复触发单测 push，PR旧运行可取消，产物保留7天，未更改审批/保护/付费设置。

不直接合并旧 PR29 来覆盖 main 新增的真实 DoH/浏览器路径。原版本0.2.1维护候选、历史419项及更早无步骤CI属于历史证据；本批必须以当前组合源码重新验证，不相加成绩。原分支保留，整合 PR 合并后再归档替代关系。

## 本轮未交付部分

公开任务停止/继续接口的工作台运行时写入被工具安全检查拦截，没有提交或打包该实验，也没有换接口/包装绕过。公开任务恢复仍属于 #30/H4、#9/R3。高级 collection.py 的工作区 DoH 接线缺口也未改变。当前网络/站点及长期任务剩余范围见 DELIVERY_STATUS.md。

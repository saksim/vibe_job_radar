# 本轮采集、恢复和验收改动的组合检查

Refs #46 / #57。基线为实际已合并的 main@ec4587992113848be879da2fbc4cebdac62d10e2。本分支整合以下功能 PR 供组合验收，远程 main、默认采集后端和实站认证状态均未因此改变。各功能判断及单独测试历史仍保留在原 Issue/PR。

| PR | 纳入的精确提交 | 范围 |
|---|---|---|
| #62 | `0d85f554818779f9445ad8e3d618a26a118910b7` | 正常登录接续、猎聘专用取数、小批身份/分页、检查点、取数漏斗 |
| #66 | `75d8cf5b9f7d0a1732de0420da5f88ad91df48ff` | 高级采集与 CLI 消费工作区网络策略 |
| #67 | `25cac6e2d0566995e8164231d85474defeac565b` | Windows 本地 POST 拒绝可靠返回403 |
| #69 | `0ad335d174ff17ff8754ad4b7ffa68937b136f03` | 公开任务停止、重启后明确继续、缓存/报告保护 |
| #70 | `28922011f54fd5d061eb4e7a183554719d68276c` | 同源能力矩阵、历史证据绑定、工作区升级回退验证 |
| #71 | `f23abbda1f0aa4c27914f99de0f8b4bfb60a0cbf` | #54 范围预登记、离线观测账本和原证据核对 |

## 合并判断

- `GuidedService._load` 保留 #62 的持锁与严格检查点解码。不能因为 #70/#71 也修了 Windows 读取竞态，就退回未校验的 JSON 加载。并发测试协调实际文件打开，覆盖新旧读取形式。
- `Registry.describe` 同时保留单次正常密码登录能力字段和 #70 的受控/实站分级元数据；新增字段不会把未认证站点升级。
- 猎聘适配器与 native 契约已变化，原9月18日的验证记录不直接移用。#70 已分别记录9月18日旧定义和9月23日 #62 的精确已测试定义；本分支按当前定义重新生成文档表格。后来再扩展主机/契约仍失去匹配证据。
- 公开任务恢复浏览器脚本保留取消/明确继续、缓存、分页和390px检查；冲突只涉及溢出时的诊断文本，选用能显示具体元素与滚动宽度的版本，并保留局部截图。
- 交付清单继续区分已合并 main 与待合并 PR。组合分支存在不等于功能已发布。
- #71 首轮 Windows TLS 完整回归遗漏 `tzdata`，新 head 已补齐。保留原19项报错的 CI 历史，未跳过新测试。
- 首轮1541项组合回归发现历史错误报告测试的生产者被新的严格检查点更早拒绝；修正人工旧报告的构造方式以继续核对读入拒绝，保持生产校验。之后27项账本/能力/并发组合测试通过。
- #70 新 CI 的网络设置脚本在严格 CSP 下以字符串轮询触发 `unsafe-eval` 拒绝。改用控件状态断言，并明确检查 CSP 保持 `script-src 'self'` 且无 `unsafe-eval`；真实 Edge 同意/跨页保存/撤销/重载通过，零外部请求。该测试修复同步至 #71。

## 验证方式与已知边界

组合源码运行完整离线测试、真实 Edge/Chromium 受控浏览器流程、源码资格检查和旧源码→新源码→旧源码兼容试验；精确 head 的结果与 CI 链接记录在集成 PR。报告及临时材料不纳入源码。

本机已执行的公开任务恢复和三进程兼容试验通过：原数据库记录、报告、个人证据、频次库及人工 Cookie 样本保留；没有访问真实招聘来源。完整组合测试若失败，修复所属功能 PR 后再次整合，不拿单个分支的绿灯替代组合结果。

真实猎聘主页面转空白仍由 #63 跟踪，没有正常账号登录和完整 JD 的实站证明。#54 的30条/3个真实日期、全部尝试与人工核对尚未完成；BOSS #55、51job #56 的专用契约与实站验收也未完成。当前组合不能被称为全站自动爬取已打通。

## 审查与回退

集成 PR 保持草稿用于组合验收，是否合并由用户审核决定，不自动关闭父目标或发布。回退前停止服务、完整备份工作区，保留原报告与频次库；兼容验证使用人工临时材料，不修改用户工作区。原生模式仍显式选择，未知页面、规则拒绝或人工验证仍停止，不换身份或通道掩盖失败。

# 网络兼容：已合并范围与仍待验收的缺口

核对日期 2026-09-23，当前已合并基线为 main@ec4587992113848be879da2fbc4cebdac62d10e2（PR61）。生产 Linux 服务器后置，普通用户本机获取不要求先部署本产品服务。源码合并、CI、特定来源实测与所有 VPN 产品兼容是不同结论。

## 已合并

- PR31：本机公开目录查询/报告、发布方严格限频和部分结果恢复、获准 HTTP 失败任务转交浏览器，以及实际消费匿名本机 HTTP 静态代理的统一 NetworkPolicy。
- PR32：原 PR26 的 SOCKS5 协议及28项原测试已整合；系统/环境提供明确 socks5:// 时自动采用，专用 HTTP/SOCKS 同时设置明确拒绝。策略标识包含协议，NO_PROXY 的 IPv6端口/IP后缀误匹配已修复。
- 静态策略为应用显式覆盖优先，否则标准库 https > all > 明确版本的 socks；自动模式按目标使用 NO_PROXY，明确专用覆盖不被排除规则偷偷改成直连。HTTP/浏览器Python桥共享连接，证书不关闭、源请求不因换路线重放。
- PR33：网页可保存并撤销加密解析偏好。默认关闭；仅系统回答全部属于198.18.0.0/15且用户明确同意时，按固定Cloudflare DoH获取有效公网地址，保持解析端/目标端TLS和目标IP绑定。正常公网DNS不增加DoH调用，不把私网/混合结果当作可修复回答。
- 解析请求沿目标原选HTTP/SOCKS/系统路线；内存缓存受域名/策略/TTL/HTTP年龄限制。取消与撤销、过期回答、硬错误被冷却或岗位缓存掩盖的问题已修复。只发送域名/类型，解析商可见域名与出口；不承诺匿名，不发送岗位正文、密码或个人材料。

## 当前仍未统一的入口

工作区加密解析偏好已经接入主干的本机公开目录、单岗位公开案例、基础页搜索与 GuidedService 浏览器请求桥。PR43 修复了浏览器回调前策略绑定，PR42 增加 Windows 原生证书链验证。**高级 collection.py 的 URL/Search/Feed 与 CLI 接线在 PR66 已实现，当前18项远端检查全绿，仍待合并**。主干高级页显示设置不能当作该批次已接通。

CLI 能使用静态代理不等于完整工作区代理偏好已统一；PR66 处理已有 DoH 偏好的实际接线，完整范围继续由 #23/N3 跟踪。历史文档中的工具拒绝是旧记录，不是本轮提交阻断。

## 仍待实现或现场验收

其他 Fake-IP 网段、PAC、代理认证/代理DNS、VM宿主机入口、企业分流/浏览器扩展网络、完整网页网络策略和实际 VPN/TUN 产品矩阵仍未全部覆盖。匿名 SOCKS5 不是 socks5h、SOCKS4 或 UDP。

BOSS、猎聘和51job的正常登录、列表/正文、动态页面、过期会话、跨日期样本尚未完成实站认证。固定公开目录和模拟本机映射的真实外网检查不能替代它们。PR61 已合并选择性本机 Cookie 保存/恢复，但不证明账号有效；长期调度、多人配额和免安装发行仍未交付。

## 验证与本批工作边界

PR33对应head已完成668项本地及七组合CI、受控Chromium、真实公开GET与真实DoH检查；模拟的是本机映射，不是生成上游正文，仍不是特定VPN产品现场测试。已合并main的单测与安装健康检查也已真实执行成功，具体运行标识记录在PR/Issue。

原 PR29 的源码绑定验收已由 PR34 合并。公开任务停止/确认继续已在 PR69 提交并完成18项远端检查，待合并；它不增加来源访问权限或网络覆盖。PR67 的 Windows 拒绝响应修复17项远端检查通过，待合并。后续提交须单独验证，不能继承这些 head 的绿灯。

实现细节见 [SOCKS整合](SOCKS_POLICY_INTEGRATION.md)、[Fake-IP解析](FAKE_IP_RESOLUTION.md)、[Fake-IP复审](FAKE_IP_REVIEW_2026_09_16.md)。原22项与H类进度见 [完整交付清单](DELIVERY_STATUS.md)。

# 猎聘登录界面配置读取

`2961c77` 自身21/21与实际exe原生组件检查通过后，2026-09-25 16:04 UTC唯一实站观察确认热门词已本地阻断且没有中止任务；新的首次失败发生在入口34.593秒，精确关联到 `api-c.liepin.com/api/com.liepin.pupa.get-pc-login-scan-config` 的OPTIONS，声明POST。尚未输入关键词、提交账号、得到新JD或报告；原失败保持。

同一已保存的[官网common脚本](https://concat.lietou-static.com/fe-www-pc/v6/js/common.0081c3a9.js)，SHA256 `865b29d63791601dbfd17e6f5186bf235d20a1fc7326e8b36007b42ec5aa9f75`，在登录容器挂载的独立effect中以不带业务参数的POST读取此配置，只把返回的graySwitch写入UI状态。用户选择扫码分类时，它决定普通或微信二维码类型；该配置调用自身不生成二维码、不轮询认证、不提交账号。该Promise链没有热门词接口的空catch，不能将两者视为同一种可选失败处理。

本次只支持精确主机、路径的POST/OPTIONS，保留发布方原生响应与选择。Origin与预检头沿用已审查同站契约，GET、文档、其他路径/主机仍不允许。配置在正常页面登录容器初始化时读取，不要求先进入密码认证动作；其请求角色标为login以排除业务正文观察，不保存响应体为岗位数据或账号认证证据。这里login计数是请求分类，真实提交次数仍由原登录动作配额计量；读配置不使用一次密码提交额度。

原robots、TLS、请求额度、取消、错误状态和CORS检查保持。发布方拒绝配置预检就停止，不伪造默认配置、不让POST或依赖搜索继续。密码API仍要求原认证模式；二维码生成/轮询、短信、注册、改密与其他未适配操作没有因此获得权限。配置值不改变用户关键词，也不能产生岗位卡片或“已认证”结论。

新回归先在旧代码上复现拒绝，覆盖正常认证前读取、原请求计量、robots/CORS、响应不进岗位队列、未知路径与密码权限隔离和403停止。人工HTTPS用真实浏览器接收服务端配置，再原表单/完整JD/报告；拒绝场景验证配置POST与后续搜索POST都未发送。原搜索/城市预检拒绝断言仍按搜索端点核对，其间允许的新配置读取单独计量。实站账号与搜索结果仍待新提交自身资格及独立实测。
